import { makeStyles } from "@fluentui/react-components";

const Styles = makeStyles({
    dialogSurface: {
        width: '50vw',
        maxWidth: '50vw',
        top: '-25vw'
    }
});

export default Styles;